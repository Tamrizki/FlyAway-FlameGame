class Consts {
  static const gameSpeed = 180.0;
  static const groundHeight = 100.0;
  static const obstacleInterval = 2.5;
  static const playerVelocity = 300;
  static const gravity = -100.0;
}

enum Movement { up, down, middle }

enum ObstaclePotition { top, bottom }
